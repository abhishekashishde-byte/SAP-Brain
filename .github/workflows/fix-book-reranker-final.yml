name: Fix book reranker final
on:
  push:
    branches: [main]
    paths: ['.github/workflows/fix-book-reranker-final.yml']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Apply patch
        run: python3 .github/scripts/fix-book-reranker-final.py
      - name: Verify
        run: node --check api/chat.js
      - name: Commit
        run: |
          rm -f .github/workflows/fix-book-reranker-final.yml .github/scripts/fix-book-reranker-final.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add api/chat.js .github/workflows/fix-book-reranker-final.yml .github/scripts/fix-book-reranker-final.py
          git commit -m "Fix live book reranker execution and audit output"
          git push origin main
